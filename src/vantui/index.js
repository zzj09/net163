import Vue from 'vue';
import { Button,Stepper,Search,Swipe, SwipeItem,Icon,Tag,List,Cell,DropdownMenu, DropdownItem } from 'vant';


Vue.use(Stepper)
.use(Button)
.use(Search)
.use(Swipe)
.use(SwipeItem)
.use(Icon)
.use(Tag)
.use(List)
.use(Cell)
.use(DropdownMenu)
.use(DropdownItem)